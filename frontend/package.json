{
  "name": "collections-frontend",
  "version": "1.0.0",
  "description": "Elm frontend for Collections Manager",
  "scripts": {
    "build": "elm make src/Main.elm --output=../dist/elm.js --optimize",
    "build:dev": "elm make src/Main.elm --output=../dist/elm.js",
    "watch": "elm-watch hot",
    "format": "elm-format src/ --yes"
  },
  "devDependencies": {
    "elm": "^0.19.1-6",
    "elm-format": "^0.8.7",
    "elm-watch": "^1.1.1"
  }
}
